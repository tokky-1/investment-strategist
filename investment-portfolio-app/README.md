# Investment Portfolio (Demo)

Small React + Vite (SWC) demo implementing the frontend spec for a portfolio experience.

Quick start:

1. npm install
2. npm run dev

Notes:
- Uses Material UI for UI primitives, Recharts for charts and Framer Motion for small animations.
- This is a frontend demo that simulates portfolio generation and loading states per the specification.
